var win = Ti.UI.createWindow();

var navWin = Ti.UI.createWindow({
	title: "Comicbook Heroes and Villian",
	modal: true,
	backgroundColor: "#fff"
});

var navGroup = Ti.UI.iPhone.createNavigationGroup({
	window: navWin
	
});
	



var mediaFile = require('char');

navWin.add(table);
win.add(navGroup);
win.open();
